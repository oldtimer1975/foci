import React from "react";
import { View, Text, StyleSheet } from "react-native";

export default function DebugTabsEntry() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>OKOSFOCI — DEBUG (app/(tabs)/index.tsx)</Text>
      <Text style={styles.sub}>Ez a fájl most az app/(tabs)/index.tsx</Text>
      <Text style={styles.note}>Ha ezt látod iPhone-on és Androidon is, akkor ugyanahhoz a bundlerhez csatlakoznak.</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: "center", alignItems: "center", padding: 20, backgroundColor: "#061219" },
  title: { color: "#45eba5", fontSize: 22, fontWeight: "800", textAlign: "center", marginBottom: 8 },
  sub: { color: "#fff", fontSize: 14, textAlign: "center" },
  note: { color: "#ddd", fontSize: 12, marginTop: 12, textAlign: "center" },
});
